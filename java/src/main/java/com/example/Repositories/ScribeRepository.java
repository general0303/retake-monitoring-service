package com.example.Repositories;

import com.example.Entities.Scribe;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScribeRepository extends JpaRepository<Scribe, Integer> {
}
